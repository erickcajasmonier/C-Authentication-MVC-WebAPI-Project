@model IEnumerable<DeleiteVenezolano.Entities.Entities.Mesa>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Reserva.ReservaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaxPersonas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EstadoMesa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Reserva.ReservaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaxPersonas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoMesa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MesaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MesaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MesaId })
        </td>
    </tr>
}

</table>
